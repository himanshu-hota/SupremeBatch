static void solve(int arr[] , int i , int sum , MaxValue max){

      if(i >= arr.length){
           max.max = Math.max(max.max, sum);
          return;
      }

    //include
    solve(arr,i+2,arr[i] + sum, max);
    
    //exclude
    solve(arr,i+1,sum, max);


  }